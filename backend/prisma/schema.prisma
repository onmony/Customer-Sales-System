// This is a placeholder schema. No database schema will be created in Module 1.
// The actual schema will be defined in Module 2.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
